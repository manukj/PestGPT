import 'package:flutter/material.dart';

class PestInfoPage extends StatelessWidget {
  const PestInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
