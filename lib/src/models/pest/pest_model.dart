import 'package:image/image.dart';

class PestModel {
  String pestName;
  Image image;
  Color color;

  PestModel({
    required this.pestName,
    required this.image,
    required this.color,
  });
}
